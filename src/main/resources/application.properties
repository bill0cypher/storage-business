server.port=8081

#smtp-specific

#db-specific
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${db.url}
spring.datasource.username=${db.username}
spring.datasource.password=${db.password}

#app-specific
resource.core.url=${storage-core-url}
